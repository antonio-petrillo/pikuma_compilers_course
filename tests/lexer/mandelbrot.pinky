func mandelbrot(cx, cy)
    x := 0
    y := 0
    iter := 0
    max := 16
    while x*x + y*y <= 4 and iter < max do
        xtemp := x*x - y*y + cx
        y := 2 * x * y + cy
        x := xtemp
        iter := iter + 1
    end
    ret iter
end

height := 16
width := 22
yi := 0
while yi < height do
    y0 := (yi / height) * 2.0 - 1.0
    xi := 0
    while xi < width do
        x0 := (xi / width) * 3.5 - 2.5
        m := mandelbrot(x0, y0)
        if m == 16 then
            print "⚡"
        else
            print " "
        end
        xi := xi + 1
    end
    println ""
    yi := yi + 1
end